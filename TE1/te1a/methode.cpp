#include "methode.h"

void tabule(std::function<float(float)> f, const Eigen::VectorXf& X, Eigen::VectorXf& Y ){
    
    for (int i= 1; i<Y.
         size(); i++){
        
        Y(i) = y(X(i)); 

    }
    
}